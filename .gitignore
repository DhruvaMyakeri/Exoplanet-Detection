# Broken venv - pip resolves to the global install, see Project.md
.venv/

# Python
__pycache__/
*.py[cod]

# Stage 2 output - ~67 MB when complete, regenerated by build_views.py
views.h5

# Run logs
*.log

# Phase 1 local cache - ~4.5 GB of (time, flux) arrays, refetch with fetch_all.py
cache/

# Explorer build artefacts (regenerate: build_explorer_data.py then build_explorer.py)
explorer_data.json
explorer.html
